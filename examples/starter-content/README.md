Starter content bundled with the template.

Use this directory when you want a clone of the repository to boot without a separate editorial repository.

You can later point the shell to a real content root through `SITE_CONTENT_DIR` or `.local/content-source.json`.
