---
import ArticlePage from '@/components/ArticlePage.astro'
import { getDefaultArticleCanonicalPaths } from '@/lib/localized-article-pages'
import { getSectionDisabledResponse } from '@/lib/section-gate'

export async function getStaticPaths() {
  return getDefaultArticleCanonicalPaths()
}

const { post } = Astro.props
const disabledResponse = getSectionDisabledResponse('articles')

if (disabledResponse) {
  return disabledResponse
}
---

<ArticlePage post={post} />
