---
import ArticlesIndexPage from '@/components/ArticlesIndexPage.astro'
import ChallengeIndexPage from '@/components/ChallengeIndexPage.astro'
import ConceptsIndexPage from '@/components/ConceptsIndexPage.astro'
import GlossaryIndexPage from '@/components/GlossaryIndexPage.astro'
import TracksPage from '@/components/TracksPage.astro'
import TopicIndexPage from '@/components/TopicIndexPage.astro'
import { getLocalizedPaginatedIndexPaths } from '@/lib/paginated-section-pages'

export async function getStaticPaths() {
  return getLocalizedPaginatedIndexPaths()
}

const { challenges = [], concepts = [], locale, page, pageType, roadmaps = [], terms = [] } = Astro.props

if (!pageType) {
  return new Response(null, {
    status: 404,
    statusText: 'Not Found',
  })
}
---

{pageType === 'articles' ? <ArticlesIndexPage locale={locale} page={page} /> : null}
{pageType === 'tracks' ? <TracksPage locale={locale} page={page} roadmaps={roadmaps} /> : null}
{pageType === 'topics' ? <TopicIndexPage locale={locale} page={page} /> : null}
{pageType === 'concepts' ? <ConceptsIndexPage concepts={concepts} locale={locale} page={page} /> : null}
{pageType === 'glossary' ? <GlossaryIndexPage locale={locale} page={page} terms={terms} /> : null}
{pageType === 'challenges' ? <ChallengeIndexPage challenges={challenges} locale={locale} page={page} /> : null}
