---
import '../styles/global.css'
import { Icon } from 'astro-icon/components'

import { getGuideIndexHref, getGuideLocaleFromPathname } from '../lib/guide-links'
import { getGlossaryIndexHref } from '../lib/glossary-links'
import { getSearchHref } from '../lib/search-links'
import { getSiteCopy } from '../lib/site-copy'
import { getStartHereHref } from '../lib/start-here-links'
import { getTopicIndexHref } from '../lib/topic-links'

type Props = {
  description: string
  englishHref?: string | null
  portugueseHref?: string | null
  title: string
}

const {
  description,
  englishHref = null,
  portugueseHref = null,
  title,
} = Astro.props

const pageTitle = `${title} | SeniorPath`
const pathname = Astro.url.pathname
const guideLocale = getGuideLocaleFromPathname(pathname)
const copy = getSiteCopy(guideLocale)
const englishGuideIndexHref = englishHref ?? getGuideIndexHref('en')
const portugueseGuideIndexHref = portugueseHref ?? getGuideIndexHref('pt-br')
const homeHref = guideLocale === 'pt-br' ? '/pt-br' : '/'
const startHereHref = getStartHereHref(guideLocale)
const learnHref = getGuideIndexHref(guideLocale)
const topicsHref = getTopicIndexHref(guideLocale)
const glossaryHref = getGlossaryIndexHref(guideLocale)
const searchHref = getSearchHref(guideLocale)
const currentYear = new Date().getFullYear()
const alternateLocale = guideLocale === 'pt-br' ? 'en' : 'pt-br'
const localeRedirectHref = guideLocale === 'pt-br' ? englishGuideIndexHref : portugueseGuideIndexHref
const isStartHerePage = pathname === startHereHref
const isLearnPage = pathname === learnHref || pathname.startsWith(`${learnHref}/`)
const isSearchPage = pathname === searchHref
---

<!doctype html>
<html class="dark" lang={guideLocale === 'pt-br' ? 'pt-BR' : 'en'}>
  <head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1" name="viewport" />
    <link href="https://fonts.googleapis.com" rel="preconnect" />
    <link crossorigin href="https://fonts.gstatic.com" rel="preconnect" />
    <link
      href="https://fonts.googleapis.com/css2?family=Archivo+Black&family=Space+Grotesk:wght@300;400;500;700&display=swap"
      rel="stylesheet"
    />
    <title>{pageTitle}</title>
    <meta content={description} name="description" />
    <meta content={pageTitle} property="og:title" />
    <meta content={description} property="og:description" />
    <meta content="website" property="og:type" />
    <meta content="summary_large_image" name="twitter:card" />
    <meta content={pageTitle} name="twitter:title" />
    <meta content={description} name="twitter:description" />
    <slot name="head" />
  </head>
  <body data-locale-alternate={alternateLocale} data-locale-redirect-href={localeRedirectHref}>
    <header class="site-header">
      <div class="site-frame site-header__inner">
          <a class="site-brand" href={homeHref}>
            <span class="site-brand__mark">
              <Icon aria-hidden="true" class="site-brand__icon" name="lucide:waypoints" />
            </span>
            <span class="site-brand__label">{copy.header.brand}</span>
          </a>
          <nav aria-label="Primary" class="site-nav">
            <a class:list={['site-nav__link', isStartHerePage && 'site-nav__link--active']} href={startHereHref}>
              {copy.layout.startHere}
            </a>
            <a class:list={['site-nav__link', isLearnPage && 'site-nav__link--active']} href={learnHref}>
              {copy.learn}
            </a>
            <a class:list={['site-nav__link', isSearchPage && 'site-nav__link--active']} href={searchHref}>
              {copy.layout.search}
            </a>
          </nav>
          <div class="site-header__actions">
            <div aria-label="Language switcher" class="site-lang-switcher">
              <a
                class={guideLocale === 'en' ? 'site-nav__link site-nav__link--active' : 'site-nav__link'}
                href={englishGuideIndexHref}
                hreflang="en"
                lang="en"
                data-locale-choice="en"
              >
                EN
              </a>
              <a
                class={guideLocale === 'pt-br' ? 'site-nav__link site-nav__link--active' : 'site-nav__link'}
                href={portugueseGuideIndexHref}
                hreflang="pt-BR"
                lang="pt-BR"
                data-locale-choice="pt-br"
              >
                PT
              </a>
            </div>
          </div>
      </div>
    </header>
    <main class="site-shell" data-pagefind-body>
      <slot />
    </main>
    <footer class="site-footer">
      <div class="site-frame site-footer__main">
          <div class="site-footer__intro">
            <a class="site-brand" href={homeHref}>
              <span class="site-brand__mark">
                <Icon aria-hidden="true" class="site-brand__icon" name="lucide:waypoints" />
              </span>
              <span class="site-brand__label">{copy.header.brand}</span>
            </a>
            <p class="site-footer__copy">{copy.footer.description}</p>
          </div>
          <div class="site-footer__group">
            <div class="site-footer__stack">
              <a class="site-footer__link" href={startHereHref}>{copy.footer.startHere}</a>
              <a class="site-footer__link" href={learnHref}>{copy.footer.learn}</a>
              <a class="site-footer__link" href={searchHref}>{copy.footer.search}</a>
            </div>
          </div>
          <div class="site-footer__group">
            <div class="site-footer__stack">
              <a class="site-footer__link" href={topicsHref}>{copy.footer.topics}</a>
              <a class="site-footer__link" href={glossaryHref}>{copy.footer.glossary}</a>
              <a class="site-footer__link" href="/terms">{copy.footer.terms}</a>
              <a class="site-footer__link" href="/privacy">{copy.footer.privacy}</a>
            </div>
          </div>
      </div>
      <div class="site-footer__bottom">
        <div class="site-frame">
          <p class="site-footer__meta">© {currentYear} {copy.footer.title}. {copy.footer.rights}</p>
        </div>
      </div>
    </footer>
  </body>
</html>

<script is:inline>
  const localeChoiceLinks = [...document.querySelectorAll('[data-locale-choice]')]

  localeChoiceLinks.forEach((link) => {
    link.addEventListener('click', () => {
      const value = link.getAttribute('data-locale-choice')
      if (!value) {
        return
      }

      try {
        window.localStorage.setItem('seniorpath.site.locale-preference.v1', value)
      } catch {}
    })
  })

  try {
    const savedLocalePreference = window.localStorage.getItem('seniorpath.site.locale-preference.v1')
    const currentLocale = document.documentElement.lang === 'pt-BR' ? 'pt-br' : 'en'
    const alternateHref = document.body.getAttribute('data-locale-redirect-href')
    const alternateLocale = document.body.getAttribute('data-locale-alternate')
    const browserPrefersPortuguese =
      navigator.language?.toLowerCase().startsWith('pt') ||
      navigator.languages?.some((value) => value.toLowerCase().startsWith('pt'))
    const preferredLocale = savedLocalePreference ?? (browserPrefersPortuguese ? 'pt-br' : 'en')

    if (!savedLocalePreference && preferredLocale !== currentLocale && alternateHref && alternateLocale === preferredLocale) {
      window.location.replace(alternateHref)
    }
  } catch {}
</script>
